# QR code + URL shown on Slide 13, for audience phones to reach the web upload app.
NEXT_NGROK_WEB_APP_URL=

# Where this presentation reads posts from (List + Slide 14's live SSE stream).
# The presentation and go-api normally run on the same machine, so localhost is enough.
NEXT_PUBLIC_API_URL=http://localhost:8080
